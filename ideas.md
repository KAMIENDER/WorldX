* 世界本身的状态是不是也应该是动态变化的，会受到时间的影响和角色动作的影响？（测试中）
* 角色的设定具体有哪些，可以更真实吗？（测试中）    
* 角色的状态是不是应该会随时间改变，比如饥饿值这些，现在会变吗（测试中）
* 需要增加保存和加载的功能（已完成）
* 由于下一个 tick 本身请求 llm 和落盘记忆有耗时，我希望做一个类似将上一轮 tick 的动作按照上一轮 llm 返回的结果的顺序，均匀分布在下一个 tick 的耗时过程中，让下一个 tick 的过程不要让人看着要等很久，至于下一个 tick 可能需要的耗时，可以用最后三个 tick 的平均耗时来预估。也不要同步播放多组不同人之间的对话，会看不过来
* 后端进行 tick 的预生成，来增加流畅度和体验（测试中）
* 角色的动作有哪些是不是也可以更真实，感觉也不一定要预设的，直接让 llm 生成是不是也可以？毕竟 llm 是有通用常识的（测试中）
* 角色的在固定时间可移动的距离是固定的，需要根据角色的基本属性进行计算（测试中）
* 角色需要能够选择结束这一天，比如睡到什么时候这样，我不确定现在世界的结束一天是怎么定义的（测试中）
* 进一步优化体验，对话组并发过程中 如果有组提前完成了，那么优先播放该组（测试中）
* 需要有我们附身的功能，可以进行沉浸式的体验，我们可以控制角色，也可以不控制角色，我们进行控制的时候，可以有专门的对话框，去显示和当前聊天对象的历史上下文，也要可以进行动作

* 物体的交互动作是不是也可以动态生成的，但是为了防止上下文过大，是不是需要限定物体只有在角色一定范围内的才能交互？但是好像有需要让角色能够感知到一定范围外的物体（比如老远就看到一堆金子）？这种怎么做结合呢
* 需要支持多场景，角色可以移动到别的场景，如果是现在没有的场景需要触发一次场景生成，生成场景的上下文需要根据世界设定和大家的记忆去生成。需要注意场景之间的连接点（交互点），由于场景可能会越来越多，所以如果没有人的场景 我们就不在每个 tick 去更新状态了，会浪费资源，只有在有人进入的时候才会在下个 tick 来重新触发该场景的更新。
* 多场景的情况下，世界的定义可能就不太一样了，是基于场景之上的更大的概念，世界更新的频率要低于场景，但是没有人的限制
* 多场景生成的时候可能会涉及到新的角色的生成，这个也需要看看怎么处理